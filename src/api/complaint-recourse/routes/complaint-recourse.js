'use strict';

/**
 * complaint-recourse router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::complaint-recourse.complaint-recourse');
