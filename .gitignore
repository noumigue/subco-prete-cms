# dependencies
node_modules/

# build outputs
.tmp/
.cache/
.strapi/client/
build/

# environment
.env
.env.*
!.env.example

# runtime uploads
public/uploads/*
!public/uploads/.gitkeep

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local Strapi data exports
strapi-editorial-data.sql
exports/

# OS junk
.DS_Store

# Comptes internes de provisionnement (données sensibles, hors VCS)
scripts/internal-users.json

# Scripts de campagne lances depuis un poste de travail : ils contiennent des adresses
# de candidats REELS en dur (constitution des listes lors de la Cohorte 1). Les commiter
# graverait des donnees personnelles dans l'historique Git, quasi impossible a purger.
# Ils sont de toute facon inoperants hors du serveur : le relais SMTP filtre par IP.
scripts/campagne/
